docker-compose up -d

### 한국어 형태소 분석기(Nori) 설치
- docker exec -it opensearch-node ./bin/opensearch-plugin install analysis-nori
- docker-compose restart
### opensearch 대화 라이브러리(가상)
- pip install opensearch-py

### 인덱스(DB 테이블) 생성 스크립트 작성
- init_opensearch.py

## v2.1 fastapi
- pip install fastapi uvicorn
> FastAPI: 서버를 만드는 도구 (Django 같은 녀석인데 훨씬 빠름) `http://127.0.0.1:8000/docs`
> Uvicorn: 만든 서버를 실행시켜주는 엔진

