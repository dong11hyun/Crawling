# B2_crawling 프로젝트 실행 과정

> 동료가 그대로 따라할 수 있도록 정리한 실행 가이드  
> 마지막 업데이트: 2026-01-15

---

## 📋 사전 준비

- Docker Desktop 설치 및 실행
- Python 3.11+ 및 venv 가상환경
- Git (선택)

---

## Week 1, Day 1: PostgreSQL 환경 구축

### Step 1: 프로젝트 클론 또는 폴더 이동

```bash
cd C:\B2_crawling
```

### Step 2: Docker 컨테이너 실행

```bash
docker-compose up -d
```

**실행되는 서비스**:
| 서비스 | 컨테이너명 | 포트 | 설명 |
|--------|-----------|------|------|
| OpenSearch | opensearch-node | 9201 | 검색 엔진 |
| Dashboards | opensearch-dashboards | 5601 | OpenSearch GUI |
| PostgreSQL | musinsa-postgres | 5434 | 원본 데이터 저장소 |
| pgAdmin | musinsa-pgadmin | 5050 | PostgreSQL GUI |

### Step 3: 컨테이너 실행 확인

```bash
docker ps
```

4개의 컨테이너가 모두 `Up` 상태인지 확인.

### Step 4: pgAdmin 접속 및 PostgreSQL 연결

1. 브라우저에서 접속: **http://localhost:5050**

2. 로그인:
   ```
   Email: admin@admin.com
   Password: admin
   ```

3. 좌측 **Servers** 우클릭 → **Register** → **Server...**

4. **General** 탭:
   - Name: `musinsa DB` (표시용 이름, 아무거나 OK)

5. **Connection** 탭:
   ```
   Host name/address: musinsa-postgres
   Port: 5432
   Maintenance database: musinsa_db
   Username: crawler
   Password: password
   ☑ Save password
   ```

6. **Save** 클릭

7. 연결 확인:
   - Servers → musinsa DB → Databases → musinsa_db → Schemas → public → Tables
   - Tables 폴더가 비어있으면 정상! (아직 테이블 미생성)

---

## 접속 주소 요약

| 서비스 | URL | 용도 |
|--------|-----|------|
| pgAdmin | http://localhost:5050 | DB 관리 GUI |
| OpenSearch Dashboards | http://localhost:5601 | 검색 데이터 조회 |
| FastAPI Swagger | http://localhost:8000/docs | API 테스트 (서버 실행 시) |

---

## 문제 해결

### 포트 충돌 시
```bash
# 사용 중인 포트 확인
netstat -ano | findstr :5434

# docker-compose.yml에서 포트 변경 후 재시작
docker-compose down
docker-compose up -d
```

### 컨테이너 로그 확인
```bash
docker logs musinsa-postgres
docker logs musinsa-pgadmin
```

### 전체 초기화 (데이터 삭제)
```bash
docker-compose down -v  # -v 옵션: 볼륨도 삭제
docker-compose up -d
```

---

## 다음 단계

- **Day 2**: SQLAlchemy 모델 정의 및 테이블 생성
- **Day 3**: CRUD API 구현
